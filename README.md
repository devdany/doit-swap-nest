### Doit swap 백엔드 입니다.

### 환경변수가 추가되어야 합니다.
```
ETHEREUM_PROVIDER_URL=url for ethereum provider
ETHERSCAN_API_URL=erherscan network api url for use (ropsten, mainnet..)
ETHERSCAN_API_KEY=etherscan api key
BSC_PROVIDER_URL=url for bsc provider
BDOI_HOLDER_ADDRESS=bsc token holer address
BDOI_HOLDER_PRIVATE_KEY=bsc token holder private key
ESTIMATED_FEE_RATIO=fee ratio
DATABASE_HOST=db host
DATABASE_PORT=db port
DATABASE_USERNAME=db username
DATABASE_PASSWORD=db password
DATABASE=database name
```